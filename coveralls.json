{
  "skip_files": ["test/support/.*"],
  "skip_file_reasons": {
    "test/support/.*": "Test-only targets and fixture builders are not production coverage surfaces."
  },
  "coverage_options": {
    "minimum_coverage": 95
  }
}
