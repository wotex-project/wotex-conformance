# Agent instructions

Read and follow `CLAUDE.md` before changing this repository. It is the
canonical contract. Apply the matching `.claude/rules/*` and
`.claude/skills/*/SKILL.md` files before editing their paths.

Do not duplicate this contract in client-specific instruction files.
